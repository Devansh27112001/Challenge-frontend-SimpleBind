import React from "react";

// Option can be genre, author, title, publicationDate

const FilterValueInput = ({ option }) => {
  option === "title" && <></>;

  return <div>FilterValueInput</div>;
};

export default FilterValueInput;
